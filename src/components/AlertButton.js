function AlertButton() {
    return (
        <div>
            <h1>Alert Button</h1>
            <button onClick={() => alert("You clicked the Alert Button")}>
                Alert Button
            </button>
        </div>
    )
}

export default AlertButton